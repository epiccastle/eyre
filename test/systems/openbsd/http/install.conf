# install.conf for OpenBSD autoinstall
Choose your keyboard layout = us
System hostname = myhost
Network interface to configure = vio0
IPv4 address for vio0 = autoconf
IPv6 address for vio0 = autoconf
Network interface to configure = done
Password for root account = root-access-please
Start sshd(8) by default = yes
Do you expect to run the X Window System = no
Change the default console to com0 = no
Setup a user = no
Allow root ssh login = yes
What timezone are you in = Australia/Perth
Which disk is the root disk = sd0
Encrypt the root disk with a (p)assphrase or (k)eydisk = no
Use (W)hole disk MBR, whole disk (G)PT or (E)dit = W
#An EFI/GPT disk may not boot. Proceed = yes
Use (A)uto layout, (E)dit auto layout, or create (C)ustom layout = a
Location of sets = cd0
Set name(s) = -x*
Directory does not contain SHA256.sig. Continue without verification = yes

# Which network interface = em0
# IPv4 address for em0 = dhcp
# IPv6 address for em0 = none
# Password for root = $2b$10$XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
# Public ssh key for root = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx user@host
# Change the default console to com0 = yes
# Setup a user = myuser
# Full name for user myuser = My User
# Password for user myuser = $2b$10$YYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYY
# Public ssh key for user myuser = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy user@host
# Allow root ssh login = no
# What timezone are you in = UTC
# Which disk is the root disk = sd0
# Use DUID for disklabel = yes
# Use (W)hole disk or (E)dit the MBR = whole

# URL to autopartitioning template for disklabel =
# Set name(s) = -all
# Which speed should com0 use = 9600
# Location of sets = http
# HTTP/FTP proxy URL = none
# HTTP Server = cdn.openbsd.org
# Server directory = pub/OpenBSD/7.5/amd64
# Checksum test for ... failed. Continue anyway = yes
# Continue without verification = yes
# Do you want to continue without verification = yes